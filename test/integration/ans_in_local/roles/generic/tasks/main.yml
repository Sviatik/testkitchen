---
- name: install generic package
  yum: name={{ item }} state=present
  with_items:
      - epel-release
      - wget
      - curl
      - vim
      - tar
      - tree
      - unzip
      - zip
      - tmux
      - git

